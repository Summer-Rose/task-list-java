<h3>Your rectangle's length is $myRectangle.getLength()</h3>
<h3>Your rectangle's width is $myRectangle.getWidth()</h3>

#if ($myRectangle.isSquare())
	<h3>Your rectangle is a square!</h3>
#else 
	<h3>Your rectangle isn't a square!</h3>
#end